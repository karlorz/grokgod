# Design

See vault `projects/grokgod/architecture/00-wrapper-vs-clawgod.md`.

Copy: wrapper + version stamp + re-apply after official update.

Do not copy: Bun extract, `cli.js` regex, Claude feature unlocks.

v1 apply target: `~/.grok/installed-plugins/**/plugin.json` skill path fields.
