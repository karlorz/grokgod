diff --git a/crates/codegen/xai-grok-agent/src/config.rs b/crates/codegen/xai-grok-agent/src/config.rs
index fc46a7bd..8045a79f 100644
--- a/crates/codegen/xai-grok-agent/src/config.rs
+++ b/crates/codegen/xai-grok-agent/src/config.rs
@@ -1223,6 +1223,20 @@ fn default_output_byte_limit() -> usize {
 fn default_true() -> bool {
     true
 }
+/// Model-level tool restrictions configured under `[model."<id>".tools]`.
+#[derive(Debug, Clone, Default, PartialEq, Eq, serde::Serialize, Deserialize)]
+pub struct ModelToolsConfig {
+    #[serde(default, skip_serializing_if = "Vec::is_empty")]
+    pub deny: Vec<String>,
+    #[serde(default, skip_serializing_if = "Vec::is_empty")]
+    pub allow: Vec<String>,
+}
+
+fn is_always_on_meta_tool(id: &str) -> bool {
+    let short = short_tool_name(id);
+    short == search_tool::SEARCH_TOOL_NAME || short == use_tool::USE_TOOL_NAME
+}
+
 /// Strip a tool id's `Namespace:` prefix, yielding its short name.
 pub(crate) fn short_tool_name(id: &str) -> &str {
     id.rsplit(':').next().unwrap_or(id)
@@ -1329,8 +1343,35 @@ impl AgentDefinition {
     }
 }
 impl AgentDefinition {
+    /// Merge model-level `tools` deny/allow lists into the session-clamp state.
+    /// Deny lists union with existing denials; allow lists intersect.
+    pub fn merge_model_tools(&mut self, tools: &ModelToolsConfig) {
+        if !tools.deny.is_empty() {
+            let mut existing = self.session_tools_denylist.take().unwrap_or_default();
+            for d in &tools.deny {
+                if !existing.iter().any(|e| e == d) {
+                    existing.push(d.clone());
+                }
+            }
+            self.session_tools_denylist = Some(existing);
+        }
+        if !tools.allow.is_empty() {
+            let model_allow = &tools.allow;
+            self.session_tools_allowlist = match self.session_tools_allowlist.take() {
+                Some(existing) => {
+                    let intersected: Vec<String> = existing
+                        .into_iter()
+                        .filter(|e| is_always_on_meta_tool(e) || tool_id_matches(model_allow, e))
+                        .collect();
+                    Some(intersected)
+                }
+                None => Some(model_allow.clone()),
+            };
+        }
+    }
+
     /// Whether `id` passes the session-operator clamp: denylist wins, then an unset allowlist allows all.
-    pub(crate) fn session_tools_allowed(&self, id: &str) -> bool {
+    pub fn session_tools_allowed(&self, id: &str) -> bool {
         if self
             .session_tools_denylist
             .as_deref()
@@ -1338,6 +1379,9 @@ impl AgentDefinition {
         {
             return false;
         }
+        if is_always_on_meta_tool(id) {
+            return true;
+        }
         self.session_tools_allowlist
             .as_deref()
             .is_none_or(|a| tool_id_matches(a, id))
@@ -1345,7 +1389,7 @@ impl AgentDefinition {
     /// Whether a hosted/server-side tool `id` survives the agent's own `disallowed_tools`/`tools` and the session clamp.
     /// Hosted tools aren't in `tool_config`, so they're gated by name here.
     /// The gate is strict: no compat-name mapping or unresolved-entry fallback like the function path.
-    pub(crate) fn hosted_tool_allowed(&self, id: &str) -> bool {
+    pub fn hosted_tool_allowed(&self, id: &str) -> bool {
         if tool_id_matches(&self.disallowed_tools, id) {
             return false;
         }
diff --git a/crates/codegen/xai-grok-agent/src/lib.rs b/crates/codegen/xai-grok-agent/src/lib.rs
index 647f765e..4ae828a0 100644
--- a/crates/codegen/xai-grok-agent/src/lib.rs
+++ b/crates/codegen/xai-grok-agent/src/lib.rs
@@ -18,6 +18,7 @@ pub use agent::Agent;
 pub use builder::AgentBuilder;
 pub use compaction::CompactionPolicy;
 pub use config::AgentDefinition;
+pub use config::ModelToolsConfig;
 pub use config::preset_names;
 pub use config::toolset_for_preset;
 pub use config::workspace_grok_build_toolset;
diff --git a/crates/codegen/xai-grok-shell/src/agent/config.rs b/crates/codegen/xai-grok-shell/src/agent/config.rs
index f039f4cb..da6bee6b 100644
--- a/crates/codegen/xai-grok-shell/src/agent/config.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/config.rs
@@ -3753,6 +3753,7 @@ fn default_models(endpoints: &EndpointsConfig) -> IndexMap<String, ModelEntryCon
                 reasoning_efforts: m.reasoning_efforts,
                 variants: m.variants,
                 supports_backend_search: m.supports_backend_search,
+                tools: None,
                 compactions_remaining: m.compactions_remaining,
                 compaction_at_tokens: m.compaction_at_tokens,
                 show_model_fingerprint: m.show_model_fingerprint,
@@ -3864,6 +3865,9 @@ pub struct ModelEntryConfig {
     pub supported_in_api: bool,
     #[serde(default, skip_serializing_if = "is_false")]
     pub supports_backend_search: bool,
+    /// Per-model tools deny/allow lists (`[model."<id>".tools]`).
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    pub tools: Option<xai_grok_agent::ModelToolsConfig>,
     /// Per-model config for the `x-compactions-remaining` header; `None` disables it.
     #[serde(default, skip_serializing_if = "Option::is_none")]
     pub compactions_remaining: Option<CompactionsRemaining>,
@@ -3937,6 +3941,7 @@ pub struct ConfigModelOverride {
     pub supports_reasoning_effort: Option<bool>,
     pub reasoning_efforts: Vec<ReasoningEffortOption>,
     pub supports_backend_search: Option<bool>,
+    pub tools: Option<xai_grok_agent::ModelToolsConfig>,
     /// Aliases must be registered in `config_model_override_parse::ALIASES`; serde rejects a table that contains both spellings otherwise.
     #[serde(alias = "send_compactions_remaining")]
     pub compactions_remaining: Option<CompactionsRemaining>,
@@ -4031,6 +4036,9 @@ impl ConfigModelOverride {
         if let Some(v) = self.supports_backend_search {
             entry.info.supports_backend_search = v;
         }
+        if self.tools.is_some() {
+            entry.info.tools.clone_from(&self.tools);
+        }
         if self.compactions_remaining.is_some() {
             entry.info.compactions_remaining = self.compactions_remaining;
         }
@@ -4126,6 +4134,9 @@ pub struct ModelInfo {
     #[serde(default, skip_serializing_if = "Vec::is_empty")]
     pub variants: Vec<ModelVariant>,
     pub supports_backend_search: bool,
+    /// Per-model tools deny/allow lists (`[model."<id>".tools]`).
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    pub tools: Option<xai_grok_agent::ModelToolsConfig>,
     /// Per-model config for the `x-compactions-remaining` header; `None` disables it.
     pub compactions_remaining: Option<CompactionsRemaining>,
     /// Per-model config for the `x-compaction-at` header; `None` disables it.
@@ -4174,6 +4185,7 @@ impl ModelInfo {
             reasoning_efforts: Vec::new(),
             variants: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
@@ -4213,6 +4225,7 @@ impl ModelInfo {
             reasoning_efforts: entry.reasoning_efforts.clone(),
             variants: entry.variants.clone(),
             supports_backend_search: entry.supports_backend_search,
+            tools: entry.tools.clone(),
             compactions_remaining: entry.compactions_remaining,
             compaction_at_tokens: entry.compaction_at_tokens,
             show_model_fingerprint: entry.show_model_fingerprint,
@@ -4982,6 +4995,7 @@ pub(crate) fn resolve_aux_model_sampling_config(
                 reasoning_efforts: Vec::new(),
                 variants: Vec::new(),
                 supports_backend_search: false,
+                tools: None,
                 compactions_remaining: None,
                 compaction_at_tokens: None,
                 show_model_fingerprint: false,
@@ -5212,6 +5226,7 @@ fn resolve_hidden_default_web_search_sampling_config(
             reasoning_efforts: Vec::new(),
             variants: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
diff --git a/crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs b/crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
index 3180d84e..4c296d38 100644
--- a/crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
@@ -707,6 +707,10 @@ mod tests {
                 default: true,
             }],
             supports_backend_search: Some(false),
+            tools: Some(xai_grok_agent::ModelToolsConfig {
+                deny: vec!["web_search".into(), "web_fetch".into()],
+                allow: vec!["read_file".into(), "grep".into()],
+            }),
             compactions_remaining: Some(CompactionsRemaining::Fixed(1)),
             compaction_at_tokens: Some(CompactionAtTokens::Fixed(100_000)),
             show_model_fingerprint: Some(true),
diff --git a/crates/codegen/xai-grok-shell/src/agent/config_tests.rs b/crates/codegen/xai-grok-shell/src/agent/config_tests.rs
index 7f1779e5..1098dc7a 100644
--- a/crates/codegen/xai-grok-shell/src/agent/config_tests.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/config_tests.rs
@@ -1054,6 +1054,7 @@ fn test_model_entry(
             supports_reasoning_effort: false,
             reasoning_efforts: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
@@ -2122,6 +2123,7 @@ fn model_info_from_config_propagates_use_concise() {
         supports_reasoning_effort: false,
         reasoning_efforts: Vec::new(),
         supports_backend_search: false,
+        tools: None,
         compactions_remaining: None,
         compaction_at_tokens: None,
         show_model_fingerprint: false,
@@ -2284,6 +2286,7 @@ fn model_info_from_config_propagates_agent_type() {
         supports_reasoning_effort: false,
         reasoning_efforts: Vec::new(),
         supports_backend_search: false,
+        tools: None,
         compactions_remaining: None,
         compaction_at_tokens: None,
         show_model_fingerprint: false,
@@ -2738,6 +2741,7 @@ fn inference_idle_timeout_propagates_to_model_info() {
         supports_reasoning_effort: false,
         reasoning_efforts: Vec::new(),
         supports_backend_search: false,
+        tools: None,
         compactions_remaining: None,
         compaction_at_tokens: None,
         show_model_fingerprint: false,
@@ -6724,6 +6728,7 @@ fn prefetch_model_entry(slug: &str, context_window: u64, api_backend: ApiBackend
             supports_reasoning_effort: false,
             reasoning_efforts: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
@@ -7569,3 +7574,184 @@ fn a_status_line_the_parser_could_not_read_in_full_reaches_grok_inspect() {
     );
     assert_eq!(cfg.ui.theme.as_deref(), Some("kanagawa"));
 }
+
+#[test]
+fn model_tools_deny_allow_deserializes_onto_model_entry() {
+    let dm = crate::models::default_model();
+    let (_, models) = resolve_models_from_toml(
+        &format!(
+            r#"
+            [model."{dm}".tools]
+            deny = ["web_search", "web_fetch"]
+            allow = ["read_file", "grep"]
+            "#
+        ),
+        None,
+    );
+    let model = models.get(dm).expect("default model should exist");
+    let tools = model
+        .info
+        .tools
+        .as_ref()
+        .expect("tools config should exist");
+    assert_eq!(
+        tools.deny,
+        vec!["web_search".to_string(), "web_fetch".to_string()]
+    );
+    assert_eq!(
+        tools.allow,
+        vec!["read_file".to_string(), "grep".to_string()]
+    );
+}
+
+#[tokio::test]
+async fn model_tools_deny_removes_web_search_and_fetch_from_built_agent() {
+    use std::sync::Arc;
+    use xai_grok_agent::builder::AgentBuilder;
+    use xai_grok_agent::config::ModelToolsConfig;
+    use xai_grok_tools::computer::local::LocalTerminalBackend;
+    use xai_grok_tools::notification::ToolNotificationHandle;
+
+    let mut def = xai_grok_agent::config::AgentDefinition::default_grok_build();
+    let model_tools = ModelToolsConfig {
+        deny: vec!["web_search".into(), "web_fetch".into()],
+        allow: vec![],
+    };
+    def.merge_model_tools(&model_tools);
+
+    let agent = AgentBuilder::new(
+        std::env::temp_dir(),
+        Arc::new(LocalTerminalBackend::new()),
+        ToolNotificationHandle::noop(),
+    )
+    .from_definition(def)
+    .build()
+    .await
+    .unwrap();
+
+    let names: Vec<_> = agent
+        .tool_definitions()
+        .await
+        .iter()
+        .map(|d| d.function.name.clone())
+        .collect();
+
+    assert!(!names.contains(&"web_search".to_string()));
+    assert!(!names.contains(&"web_fetch".to_string()));
+    assert!(names.contains(&"read_file".to_string()));
+}
+
+#[test]
+fn model_tools_deny_x_search_suppresses_hosted_x_search() {
+    use xai_grok_agent::config::ModelToolsConfig;
+
+    let mut def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    assert!(def.hosted_tool_allowed("x_search"));
+    assert!(def.hosted_tool_allowed("web_search"));
+
+    let model_tools = ModelToolsConfig {
+        deny: vec!["x_search".into()],
+        allow: vec![],
+    };
+    def.merge_model_tools(&model_tools);
+
+    assert!(!def.hosted_tool_allowed("x_search"));
+    assert!(def.hosted_tool_allowed("web_search"));
+}
+
+#[test]
+fn model_tools_allow_mode_retains_listed_builtins_and_meta_tools() {
+    use xai_grok_agent::config::ModelToolsConfig;
+
+    let mut def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    let model_tools = ModelToolsConfig {
+        deny: vec![],
+        allow: vec!["read_file".into(), "grep".into()],
+    };
+    def.merge_model_tools(&model_tools);
+
+    assert!(def.session_tools_allowed("read_file"));
+    assert!(def.session_tools_allowed("grep"));
+    assert!(!def.session_tools_allowed("write"));
+    assert!(!def.session_tools_allowed("run_terminal_cmd"));
+    // Meta tools are always allowed in allowlist mode
+    assert!(def.session_tools_allowed("search_tool"));
+    assert!(def.session_tools_allowed("use_tool"));
+}
+
+#[test]
+fn model_tools_deny_wins_when_both_deny_and_allow_present() {
+    use xai_grok_agent::config::ModelToolsConfig;
+
+    let mut def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    let model_tools = ModelToolsConfig {
+        deny: vec!["read_file".into(), "search_tool".into()],
+        allow: vec!["read_file".into(), "grep".into()],
+    };
+    def.merge_model_tools(&model_tools);
+
+    // read_file is in both -> deny wins
+    assert!(!def.session_tools_allowed("read_file"));
+    // grep is only in allow -> allowed
+    assert!(def.session_tools_allowed("grep"));
+    // write is in neither -> not allowed due to allowlist mode
+    assert!(!def.session_tools_allowed("write"));
+    // search_tool explicitly denied -> deny wins even over meta-tool bypass
+    assert!(!def.session_tools_allowed("search_tool"));
+}
+
+#[test]
+fn model_tools_global_switches_still_dominate_union_semantics() {
+    use xai_grok_agent::config::ModelToolsConfig;
+
+    let mut def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    // Agent / global disallowed_tools
+    def.disallowed_tools = vec!["web_search".into()];
+
+    let model_tools = ModelToolsConfig {
+        deny: vec!["grep".into()],
+        allow: vec![],
+    };
+    def.merge_model_tools(&model_tools);
+
+    assert!(!def.hosted_tool_allowed("web_search"));
+    assert!(!def.session_tools_allowed("grep"));
+    assert!(def.session_tools_allowed("read_file"));
+}
+
+#[test]
+fn subagent_model_resolution_gets_its_own_model_tools_lists() {
+    let (_, models) = resolve_models_from_toml(
+        r#"
+        [model."parent-model".tools]
+        deny = ["web_search"]
+
+        [model."child-model".tools]
+        deny = ["grep"]
+        "#,
+        None,
+    );
+
+    let parent_model = models
+        .get("parent-model")
+        .expect("parent model should exist");
+    let child_model = models.get("child-model").expect("child model should exist");
+
+    let mut parent_def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    if let Some(tools) = &parent_model.info.tools {
+        parent_def.merge_model_tools(tools);
+    }
+
+    let mut child_def = xai_grok_agent::config::AgentDefinition::general_purpose();
+    if let Some(tools) = &child_model.info.tools {
+        child_def.merge_model_tools(tools);
+    }
+
+    // Parent has web_search denied, grep allowed
+    assert!(!parent_def.session_tools_allowed("web_search"));
+    assert!(parent_def.session_tools_allowed("grep"));
+
+    // Child has grep denied, web_search allowed
+    assert!(child_def.session_tools_allowed("web_search"));
+    assert!(!child_def.session_tools_allowed("grep"));
+}
diff --git a/crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs b/crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
index 0de66027..ec2871b0 100644
--- a/crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
@@ -111,7 +111,12 @@ pub(crate) async fn apply(
                 agent.plugin_registry_handle.snapshot().as_deref(),
             );
             match resolved {
-                Some(def) => {
+                Some(mut def) => {
+                    if let Some(model_tools) =
+                        agent.models_manager.model_tools_config(model_id.0.as_ref())
+                    {
+                        def.merge_model_tools(&model_tools);
+                    }
                     tracing::info!(
                         session_id = %session_id.0,
                         model_id = %model_id.0,
diff --git a/crates/codegen/xai-grok-shell/src/agent/models.rs b/crates/codegen/xai-grok-shell/src/agent/models.rs
index 840e57b7..65d93d41 100644
--- a/crates/codegen/xai-grok-shell/src/agent/models.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/models.rs
@@ -608,6 +608,14 @@ impl ModelsManager {
             .unwrap_or(false)
     }
 
+    pub(crate) fn model_tools_config(
+        &self,
+        model_id: &str,
+    ) -> Option<xai_grok_agent::ModelToolsConfig> {
+        self.with_catalog_entry(model_id, |e| e.info().tools.clone())
+            .flatten()
+    }
+
     pub(crate) fn model_compactions_remaining(
         &self,
         model_id: &str,
diff --git a/crates/codegen/xai-grok-shell/src/agent/models/tests.rs b/crates/codegen/xai-grok-shell/src/agent/models/tests.rs
index 6b01792f..9f74a405 100644
--- a/crates/codegen/xai-grok-shell/src/agent/models/tests.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/models/tests.rs
@@ -2045,6 +2045,7 @@ fn make_entry_config_with_id(
         supports_reasoning_effort: false,
         reasoning_efforts: Vec::new(),
         supports_backend_search: false,
+        tools: None,
         compactions_remaining: None,
         compaction_at_tokens: None,
         show_model_fingerprint: false,
diff --git a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
index 3bae866c..f6e5b2b3 100644
--- a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
@@ -4345,6 +4345,9 @@ impl MvpAgent {
                 sampling_config,
                 origin_client.clone(),
             );
+        if let Some(model_tools) = self.models_manager.model_tools_config(&session_model_id.0) {
+            agent_definition.merge_model_tools(&model_tools);
+        }
         self.models_manager
             .apply_supported_effort(
                 &mut sampling_config,
diff --git a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/tests.rs b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/tests.rs
index 94120880..5a56b25c 100644
--- a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/tests.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/tests.rs
@@ -2678,6 +2678,7 @@ fn find_model_by_id_prefers_key_then_falls_back_to_slug() {
             supports_reasoning_effort: false,
             reasoning_efforts: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
diff --git a/crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs b/crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
index aa8d8e68..c7d84bc1 100644
--- a/crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
@@ -704,6 +704,12 @@ pub(crate) async fn run_shell_child(
     let subagent_model_id = effective_sampling_config.model.clone();
     let auto_compact_threshold_percent =
         ctx.resolve_auto_compact_threshold_percent(&subagent_model_id);
+    if let Some(model_tools) = ctx
+        .models_manager
+        .model_tools_config(effective_model_id.0.as_ref())
+    {
+        definition.merge_model_tools(&model_tools);
+    }
     let subagent_id = request.id.clone();
     let child_session_id = acp::SessionId::new(subagent_id.clone());
     let override_cwd = select_override_cwd(resume_source.as_ref(), request.cwd.as_deref());
diff --git a/crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs b/crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
index cafbd9e5..9924cb77 100644
--- a/crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
@@ -2340,6 +2340,7 @@ fn test_model_entry(model_id: &str) -> crate::agent::config::ModelEntry {
             supports_reasoning_effort: false,
             reasoning_efforts: Vec::new(),
             supports_backend_search: false,
+            tools: None,
             compactions_remaining: None,
             compaction_at_tokens: None,
             show_model_fingerprint: false,
diff --git a/crates/codegen/xai-grok-shell/src/remote/client.rs b/crates/codegen/xai-grok-shell/src/remote/client.rs
index 909bf522..47eca70e 100644
--- a/crates/codegen/xai-grok-shell/src/remote/client.rs
+++ b/crates/codegen/xai-grok-shell/src/remote/client.rs
@@ -817,6 +817,18 @@ pub(crate) fn parse_remote_model_value(
             .or_else(|| meta.and_then(|m| m.get("supportsBackendSearch")))
             .and_then(|v| v.as_bool())
             .unwrap_or(false),
+        tools: get_object(obj, "tools")
+            .or_else(|| meta.and_then(|m| get_object(m, "tools")))
+            .and_then(|v| match xai_grok_agent::ModelToolsConfig::deserialize(v) {
+                Ok(cfg) => Some(cfg),
+                Err(e) => {
+                    tracing::warn!(
+                        error = %e,
+                        "Failed to deserialize tools block from remote model; falling back to none"
+                    );
+                    None
+                }
+            }),
         compactions_remaining: obj
             .get("compactionsRemaining")
             .or_else(|| obj.get("compactions_remaining"))
